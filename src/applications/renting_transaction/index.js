import { ContractFactory } from '../../factories'
import { ApartmentRepository } from '../../repositories'

export class RentingTransaction {
    someFunction(renterId, apartmentType, startDate, expiredDate, deposit, buildingId, monthlyRentPrice) {

        ContractFactory.createNewContract(start)
    }
}